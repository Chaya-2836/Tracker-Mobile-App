import { StyleSheet, Platform } from 'react-native';

export default StyleSheet.create({
  
  
  title: {
    fontSize: 16,
    fontWeight: '600',
    marginBottom: 12,
    color: '#34495e',
    textAlign: 'center',
  },

  label: {
    fontSize: 14,
  },
  noData: {
    marginTop: 20,
    fontSize: 16,
    color: '#888',
    textAlign: 'center',
  },
});