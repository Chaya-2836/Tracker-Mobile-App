import { StyleSheet, Platform } from 'react-native';

export default StyleSheet.create({
  label: {
    fontSize: 14,
  },
  noData: {
    marginTop: 20,
    fontSize: 16,
    color: '#888',
    textAlign: 'center',
  },
});